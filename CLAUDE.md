# CLAUDE.md ·《假总统》(El Doble) Demo 开发锚点

> 本文件是 AI 协作的"宪法"。每次对话开始时会自动加载。
> 若与具体任务说明冲突，**遵循当下任务说明**；但任何与本文件冲突的偏离都要在响应里**明确标注原因**。
> 若与项目设定文档（`01_*.md`–`07_*.md`、`08_剧情/`、`99_*.md`）冲突，**设定文档优先**。本文件随设定演进。

---

## 1. 项目身份

- **项目代号**：《假总统》(*El Doble*)
- **类型**：政治危机文字向叙事游戏，单局 3–5 小时，多周目设计
- **核心命题**：当一个体制需要任何人都可以替代，那么它真正在替代的，是什么？
- **基调**：拉美黑色幽默 + 魔幻现实主义的克制使用 + 良知与现实的反复磨损
- **不要做的**：英雄逆袭爽文、二元正邪剧、揭露真相后世界变好的天真叙事

---

## 2. 开发范式（核心约定）

### 技术栈
- **纯 HTML + JavaScript 单文件可玩 demo**。打开浏览器即跑，零安装、零构建。
- **不要引入 React / Vue / Svelte 等需要构建步骤的框架**。需要复杂状态时，先用原生 JS Class + 模块化 `<script type="module">`。轻量响应式可考虑 Alpine.js 或 Petite-Vue（CDN 引入），但**只在状态复杂到 vanilla JS 难维护时**引入。
- **样式**：原生 CSS，使用 CSS 变量做主题。**不要用 Tailwind 的 utility classes**——叙事游戏的视觉气质需要手写的细节。
- **存档**：`localStorage`，序列化为 JSON。**单存档 + 自动保存**（详见 `05_多周目机制.md`）。
- **构建/部署**：单文件优先；如需拆分，资源相对路径引用，确保 `file://` 也能跑。

### 开发节奏
- **当前阶段：序幕（铜屋之夜）**——按 demo 范围从这里开始。
- **架构原则：写第一段代码就为"扩展到全作"做准备**。即使现在只实现序幕，数据结构、事件系统、派系系统都要按全作骨架来设计。**反例**：把序幕的对话硬编码进 HTML——这是禁忌。
- **数据驱动**：事件、对话、派系、数值变动都是数据（JSON / JS 对象数组），引擎只负责消费。后期才方便交给我（Keyan）或外部协作者写内容。
- **垂直切片优先**：序幕跑通后，下一步是 Acto I 的第 1 天（首次内阁会议） — 它会暴露 12 时辰系统、强制项、突发事件、晚间简报全部机制。**先把这一天的所有循环跑顺，再做内容铺陈。**

### 代码与文本语言
- **代码（变量、函数、类、文件名）**：英文，遵循常规 JS 命名（camelCase / PascalCase）。
- **代码注释、commit message、PR 描述**：中文。
- **所有面向玩家的叙事文本**：中文为主，**允许并鼓励西语 flavor**（人名、地名、派系名、报刊名、咒骂、口头禅按设定文档保留西语原文，必要处给中文译注）。
- **游戏术语（机制层概念）以中文为锚**：神智 / 伪装度 / 怀疑度 / 政治资本 / 经济 / 秩序 / 合法性 / 派系态度 / 时辰 / 隐性标记 / 机动池 等。**面向玩家的 UI 标签、规则提示、调试输出、设定文档正文一律用中文术语**；**西语只用在上一条所说的风味文字里**——不要在 UI / 术语层混用 Cordura / Verosimilitud / Sospecha 等原文（设定文档中可保留括注作为索引）。
- **不要把派系/角色名翻译成中文**。Doña Isabel 永远是 Doña Isabel，不要写成"伊莎贝尔夫人"。

---

## 3. 决策边界（AI 何时问、何时直接做）

**先问后做**（出现以下任意一项，必须征询）：
1. 引入新的依赖、库、框架。
2. 修改已实现的核心数据结构（事件 schema、状态机、派系系统）。
3. 改动玩法机制（数值轴、时辰消耗、Acto 划分等设定文档涉及的内容）。
4. 任何会影响"全作扩展性"的架构决定。
5. 不确定某段叙事文本的调性是否符合设定文档。

**直接做**（不问）：
- 命名、变量重构、文件拆分、目录结构。
- UI 微调、CSS、动效。
- 测试数据、占位文本（但要明确标注 `// TODO: placeholder` 中文化时）。
- Bug 修复（除非修复方式涉及上面"先问"的任何一条）。
- 写新事件 / 新对话的草稿——但**写完要让 Keyan review，不直接当成最终文本**。

---

## 4. 项目设定文档（Single Source of Truth）

所有玩法、世界观、文本调性的最终依据是这些文档。**写代码前若涉及对应主题，先 Read 对应文档**。

| 文档 | 涵盖内容 | 何时必读 |
|---|---|---|
| `01_世界观与派系小传.md` | 国家设定、物价表、派系小传、原住民、地理 | 写涉及任何派系/地点/经济细节的事件时 |
| `02_真总统与替身原型.md` | Báez 总统、Saulito（替身）、Susana、Vega、Estela 等核心 NPC、开局培训 | 写涉及主角内心戏、核心 NPC 对白时 |
| `03_核心玩法.md` | 12 时辰系统、Acto 结构、时刻表、突发事件、事件结构模板 | 写引擎、状态机、单日循环、UI 时 |
| `04_数值与阵营.md` | Tier 1–4 数值轴、派系态度文字标签、Verosimilitud / Cordura / Sospecha | 写涉及任何数值变动 / 派系态度 / 检定时 |
| `05_多周目机制.md` | 档案室、知识传承、下落卷宗、Cordura 0 发疯、单存档与自动保存、结局分类 | 写存档系统、Cordura 极低事件、多周目 / 结局相关时 |
| `06_主题与调性.md` | 三种声音、写作硬规则、黑色幽默 / 魔幻现实主义边界 | **写任何叙事文本前必读** |
| `07_风格样本.md` | 已成稿的样本事件（铜街人行道等），是文本调性的"金标准" | 仿写新事件时作为参考 |
| `08_剧情/` | 各 Acto 详细剧情大纲、关键标记索引、终章与结局 | 写具体某 Acto / 某天的事件时 |
| `99_秘密文档.md` | **关键剧情骨架（仅 Keyan 与 AI 可见）** — Báez 真实下落、终章分支、暗线索引 | 写涉及结局走向、长线伏笔时；**这些信息绝不可出现在玩家可见文本中** |

---

## 5. 写作硬规则（来自 06_主题与调性）

> 写叙事文本前先在脑里检查这些。AI 出错最频繁的地方就在这里。

1. **三种声音不混用**：场景叙述者（第三人称、冷静）/ Saulito 内心独白（第二人称、斜体、口语）/ 新闻广播（拟文体）。同一段里只能有一种。
2. **黑色幽默来自情境，不来自旁白**。**永远不要**写"哈哈，多么讽刺啊"这种贴脸笑的句子。
3. **魔幻现实主义是质感不是机制**。占卜婆婆的预言可以"准了一次"作为氛围，但**不能用来改写数值或解锁分支**。
4. **没有派系是"对的"**。每个派系都要有真实诉求和真实瑕疵。写派系视角时给出他们自己的"合理"。
5. **金钱要有体感**。提到数字时要有现实参照（参考 01 文档的物价表）。"四万北元"应当让玩家感受到这是 Saulito 两年的收入，不是抽象数字。
6. **政治术语谨慎**。Saulito 是没受过政治训练的街头艺人，他的内心独白里不会出现"民粹主义""寻租"这种词——除非是他从别人嘴里听到的、半懂不懂的复述。
7. **不要解释隐喻**。设定里满是双关与暗喻（Veladuria 的字源、铜元、漫长之夜）—— **让玩家自己发现，不要旁白点破**。

---

## 6. 架构骨架（demo 起步约定）

```
政治危机文字向游戏/
├── CLAUDE.md                   # 本文件
├── 01_*.md – 99_*.md            # 设定文档（设计单一真相源）
├── demo/                        # 可玩 demo 根目录
│   ├── index.html               # 入口（单文件优先）
│   ├── styles/                  # CSS（按模块拆，不引 framework）
│   ├── engine/                  # 引擎层：状态机、时间系统、数值轴、存档
│   ├── content/                 # 数据层：事件、对话、派系定义（JSON）
│   │   ├── events/              # 按 Acto / Day 组织
│   │   ├── npcs/                # 角色档案
│   │   └── factions/            # 派系定义与态度逻辑
│   └── assets/                  # 图片、音频（demo 阶段可空）
└── docs/dev/                    # 给 AI 看的开发笔记（迭代决策记录）
```

**关键数据结构（早期就要立住）**：
- `GameState`：当前 Acto / Day / Hour、数值轴（Cordura、Verosimilitud、Economía、Orden、Legitimidad、Tesoro）、派系态度表、隐性标记集合（`flags`）。
- `Event`：包含触发条件（哪天、哪时段、哪些 flag/数值前置）、文本（按三种声音分块）、选项（每个选项是 `effects: { state变动, flags新增, factionDelta, 后续事件入队 }` 的纯数据）。
- `DaySchedule`：早班简报组装、强制项 / 已申请觐见 / 死线 / 空白时段，由当日 events 与 flags 推导。

**禁止**：把文本写死在 `<div>` 里。**所有文本必须从数据层加载**，否则后期内容协作和翻译会全废。

---

## 7. AI 工作流默认行为

- **每次开新会话先扫一眼 `MEMORY.md`**（如有），但**任何具体设定都以本仓库的设定文档为准**。
- **写代码前**：相关文档先 Read 一遍。不确定的地方先 Grep 找上下文。
- **写完代码**：在浏览器跑一次（用 `mcp__workspace__bash` 起个本地 server，或确认 `file://` 能跑），**至少手动走一遍最关键路径**。不能只 commit 不验证。
- **写完叙事文本**：拿 `06_主题与调性.md` 的"硬规则"和 `07_风格样本.md` 自检一遍——三种声音是否分清？是否有贴脸笑？金钱是否有体感？
- **commit 习惯**：小步提交，每个 commit 只做一件事。message 中文，开头标注模块（例：`engine: 时辰消耗系统支持负数检定`）。
- **不擅自更新设定文档**。如果 demo 实现暴露了设定矛盾，**列出矛盾并问 Keyan**，不要单方面改 `01_*.md` ~ `99_*.md`。
- **文档改动直接打印变更日志到聊天框**：每次改 `01_*.md` ~ `99_*.md` 或本 `CLAUDE.md`（任何设定/规则类 markdown）后，把"改了什么 / 为什么 / 影响哪些下游内容"以简明 changelog 的形式输出到对话里。**文档内部不保留 changelog / 版本历史 / 更新日期段落**——这些文档只给 Keyan 和 AI 看，可读性给 AI 的优先级高于人类读者，文件里只保留"当下生效的内容"，历史用 git log 追溯即可。

---

## 8. 反模式清单（看到立刻停）

- ❌ 引入 React / Next.js / Vite 等需要构建的栈
- ❌ 把叙事文本硬编码进 HTML / JS 字符串字面量
- ❌ 用 emoji 装饰玩家可见的 UI（除非设定文档明示）
- ❌ 在玩家可见文本中泄露 `99_秘密文档.md` 的内容
- ❌ 把派系/人物名翻译成中文
- ❌ 让派系态度数字直接显示给玩家（**Tier 2 派系态度是文字标签**，不是 "+5 十二姓氏"。Tier 1 国家指标和 Tier 3 个人状态可以是数字）—— 详见 04 文档 §二 派系关系
- ❌ 写"主角觉醒、拯救国家"风格的剧情走向
- ❌ 旁白点破隐喻 / 旁白评论自己的幽默
- ❌ 在没有读过 `06_主题与调性.md` 的情况下写任何超过两句的叙事段落

---

## 9. 当前里程碑

- [ ] **M0**：搭建 `demo/` 骨架，跑通 `index.html` 空壳，确认 `localStorage` 存档能 round-trip。
- [ ] **M1**：序幕《铜屋之夜》— 单一线性场景 + 末尾"你为什么答应？"分支，验证文本调性与三种声音的渲染层。
- [ ] **M2**：Acto I 第 1 天 — 完整 12 时辰循环 + 早班简报 + 晚间简报 + 至少 1 个强制项 + 1 个突发事件，跑通核心循环。
- [ ] **M3**：派系态度系统 + 隐性标记 + 多周目存档分离。

> 当前在 M0 之前——尚未动代码。下一步建议先做 M0。
